namespace Fulbank.classes
{
    ///<summary>Class admin contains the admin's data (name, first name)
    ///</summary>
    public class Admin : Person
    {
        ///<summary>Public Admin is the constructor of the Admin class
        ///</summary>
        public Admin(int id, string name, string firstname) : base(id, name, firstname)
        {
        }
        
    }
}
